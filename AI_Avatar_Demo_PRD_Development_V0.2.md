# AI 数字人实时视频通话 Demo
## 产品需求与开发规格 · V0.2

**2026-09-11 账户与授权修订（v3，用户已全部批准）：** 增加统一登录与管理员后台。管理员统一建号、分发和重置密码；普通用户不注册、不自助改密或找回，忘记密码联系管理员。默认生成12位密码，手动设置至少12位；管理员初始测试凭据由受控环境或运维输入，只初始化一次，不写入仓库。管理员登录8小时、用户7天，可配置。同一用户可分配多个数字人，同一数字人可分配多个用户，不设五个入口上限；每个用户与人物的关联独立保存展示名、关系称呼、提示词和设定，分配时复制默认值，后续修改共享默认设定不覆盖已有分配，修改专属设定在该用户下一场通话生效。支持多账号登录，全系统同时仍最多一场通话，管理员测试不抢占。重置/停用撤销账号旧登录并结束受影响通话；解绑只影响目标用户与目标人物。已有角色和媒体保留，初始仅管理员可见。安全撤权先禁止授权，再结束通话和清理，失败保持拒绝。此段取代下文旧U01/U02及账号后移口径；其他已批准阶段修订继续有效。

**2026-09-09 角色管理与照片规范化修订（用户已批准）：** 新上传照片全部经 ZenMux 编辑，保留本人五官、轮廓、皱纹、原有笑容/露齿和背景；主要规范远景人物占比，近景尽量不改，不强制闭嘴或证件照化。主模型失败后备用模型仅尝试一次，两者失败才提示；成功后自动制作，不增加逐张确认。角色支持重命名及关系/性格/语气/背景设定，下一场生效。删除采用数据库 `DELET_OR_NOT` 软删除，保留资料和媒体，隐藏并停止该角色任务，暂不清理或提供恢复入口。点击角色后先显示绿色接听按钮，点击接听才连接语音、申请麦克风并抢占制作资源；在途云请求可保存结果，挂断后继续。此段优先于下文旧照片准入、编辑限制与数据清理描述。 详见[当前实现与验收](docs/ROLE_MANAGEMENT_NORMALIZATION.md).

**2026-09-09 当前阶段修订（用户已批准）：** 每批最多 3 张、一图一角色、总数不限；本机单用户，账号/管理员/多人并发后移，不使用摄像头。后台串行制作并为通话让路，挂断后继续；意外退出后手动重试。静音判停改为 1.2 秒；首次可监听即开始 120＋10 秒空闲计时。五秒同场重连后移，断线结束后重新呼叫为新场。数字人使用本地已验收的 DH_live_mini 实现。本段及[阶段说明](docs/BATCH_CALL_PHASE.md)优先于下文涉及当前范围的旧描述，其余长期产品要求保留。

版本状态：需求更新稿，可用于拆解开发任务；包含少量待确认边界，尚非无条件冻结的合同范围。
整理日期：2026-09-07。决策依据：已填写清单及本轮全部后续补充。
适用对象：产品负责人、客户确认人、前后端开发、测试与部署人员。

**本版核心：保留豆包实时语音，通过现成数字人服务自动创建人物、驱动画面；管理员发放账号并控制角色权限；实现单次会话记忆、一问一答、角色独占、5 秒短断恢复和视频失败后的纯语音降级。**

**阅读提示：最新补充优先于旧清单。旧版的短信注册、固定两路并发、每日三次、十秒记忆恢复、情绪表情强要求、不支持纯语音降级，均不能再作为本次开发依据。**

---

## 目录

01 文档依据与执行口径
02 产品目标与本次范围
03 关键规则总表
04 业务对象与角色关系
05 页面与用户流程
06 账号、登录与权限
07 人物自动创建与素材管理
08 用户配置、绑定与编辑保护
09 实时通话与状态流转
10 计时、记忆与五秒重连
11 音视频同步与纯语音降级
12 容量、异常与资源回收
13 开发架构与模块职责
14 建议数据模型
15 业务接口与事件契约
16 安全、日志与数据生命周期
17 参数、部署与运行配置
18 测试与验收用例
19 开发里程碑与完成定义
20 未决事项与技术验证清单
21 新旧决策追踪与变更记录

---

## 01 文档依据与执行口径

### 1.1 需求来源和优先级

| 来源 | 本文代号 | 使用方式 |
|---|---|---|
| 用户上传的 `ai_avatar_demo_decision_checklist(1).md` | S1；原条目 D001 等 | 未被后续明确修改的决定继续有效；用户已经删除的章节不恢复为必做功能 |
| 用户“现在我进行补充”的 1—14 条及末尾账号说明 | S2；补充 1 等 | 覆盖旧清单相应选项，包括自动创建、默认音色、取消短信、暂不设费用上限 |
| 用户“补充完毕”一轮中的 A2—A8、B1—B12 | S3；A3、B2 等 | 最高优先级，覆盖五秒重连、120＋10 秒、关系模型、日志、黑屏纯语音等 |
| 前述助手建议 | 仅限被用户明确接受的部分 | 未获确认的建议不自动升级为客户要求；供应商介绍不等于实际账号已验证能力 |

本文是对以上来源的整理，不加入其他会话中的 GPU 自部署路线，也不重新启用 Live2D。未重新核查第三方平台当前定价、额度或接口可用性，不将历史介绍当作已确认能力。

### 1.2 三种标记

**【已定】**：来源中明确选择，或者用户明确接受的建议。开发与测试以此为准。
**【沿用】**：旧清单明确、后续没有撤销。例如单次 15 分钟和提前 10 秒提示。
**【实现建议 / 待确认】**：为便于开发提供的方案、暂定字段或缺失参数，不代表客户新增承诺。

第 13—17 节的架构、表名、接口名和事件名是本项目的实现建议，不是豆包或即构的官方 API。验收用例中涉及未决事项的用例必须标记“阻塞/待确认”，不能擅自按某个假设通过。

### 1.3 当前最重要的两处范围歧义

**关系与额度：** S3-B2 明确取消多对多、改为多个用户对应一个角色；S3-B5 又继续描述“处理中占额度、失败不占、重复创建也占”。此前还有每账号五个入口的规则。本文按最新 B2 描述角色共享主线，但不把“每账号五个角色”或某个额度归属写成已冻结约束。详见 U01。

**编辑与撤权：** S3-B9 禁止角色通话中修改相关信息；已接受的 A6 又要求停用、解绑终止受影响通话。两者的操作顺序需要明确。本文将一般内容修改与安全撤权分开，并提供“先终止受影响通话，再完成变更”的建议流程，待确认 U02。

不应因这两点重新开放短信、长期记忆或动作等已明确删除的需求。可先推进无争议模块；相关绑定唯一性约束和撤权交互在确认后冻结。

## 02 产品目标与本次范围

### 2.1 产品定位

用于指定人员体验与技术验证的网页 Demo，不是正式收费运营平台。目标是证明：管理员从照片和角色资料出发，经自动人物创建与用户授权，使用户可以进入具有自然人脸画面的实时语音通话。

用户已完成基本的一问一答语音闭环。本次以复用该能力为前提，不以重做语音系统为目标。主线通过后再评估正式开发预算、运营费用、部署预算与使用规模。

### 2.2 必须形成的闭环

管理员登录 → 上传合格照片 → 系统自动提交人物创建 → 页面显示处理中 → 平台成功后自动可用 → 为用户设置身份、背景、口吻并授权 → 管理员可独立测试 → 用户登录看到自己的角色列表 → 点击进入数字人通话 → 正常结束并释放资源。

自动创建必须是正常流程。人工可处理失败或异常，但不能把所有人物都人工制作后再填编号的流程当作自动创建已完成。

### 2.3 本次包含与排除

| 包含 | 明确不包含 |
|---|---|
| 豆包实时语音、普通话默认音色、自动1.2 秒静音判停 | 声音克隆、方言专项优化、按住说话为主要交互 |
| 一问一答，短回答和自然短反问 | 自由插话打断、多人同场通话 |
| 单次连续通话上下文 | 退出重进或新通话恢复旧聊天、人物检索知识库 |
| 声音驱动嘴型、自然面部变化、平台自带微动作 | 指令抬手等动作、精准情绪表情、理解用户摄像头画面 |
| 正脸、肩部以上的数字人画面 | 必须展示手臂、完整真人动作复原、微信全部原生通话功能 |
| 管理员账号、用户账号、角色授权和账号数据库 | 公开注册、手机号字段必填、短信登录、自助找回密码 |
| 运行日志、时长和用量查看 | 聊天历史回放、通话录音录像、保存完整聊天正文 |
| 电脑 Chrome 主线；其他适配从简 | App、原生小程序、全浏览器或锁屏后台连续通话保证 |
| 数字人故障后黑屏继续纯语音 | 故障时无限重试、把纯语音等同于数字人效果通过 |

### 2.4 交付边界

当前无固定 Demo 截止日期；开发方保留关闭测试入口的权利。正式长期使用、费用承担、运维服务和规模承诺需要另行确认。本文的开发与测试说明，不自动恢复原文件已被删除的合同交付、无限维护或正式运营条款。

“自然、尽可能真实”的画面目标以实际人物小样、约定设备上的测试结果确认。本文不承诺任意照片都成功，也不声明达到无法辨别真假的效果。

## 03 关键规则总表

| 项目 | 当前执行规则 | 依据 |
|---|---|---|
| 人物创建 | 自动异步创建；人工仅异常兜底 | S2-1、11；S3-B4 |
| 登录 | 管理员统一发账号、设密码和重置；无手机号和短信 | S2-2、13；S3-A6 |
| 登录设备 | 可多设备登录；每个账号只能有一场通话 | S3-A6 接受建议 |
| 角色占用 | 同一角色资源一次只能被一名用户或管理员测试使用 | S2-10；S3-A4、B3 |
| 角色忙碌 | 明确提示稍后重试，不排队、不抢占 | S3-A4 |
| 角色配置 | 按共享资源管理；可为不同用户设置不同身份、背景、口吻 | S3-B2 |
| 内容修改 | 角色通话中禁止修改相关内容，提示等待空闲 | S3-B9 |
| 产品人数上限 | 取消账号总量、系统人物总量、固定两路并发和每日次数限制 | S3-A2、B7 |
| 实际运行能力 | 不承诺无限并发；外部服务额度和环境能力仍需验证 | 技术约束，不是恢复产品人数限制 |
| 判停 | 讲话后的连续1.2 秒静音结束当前发言 | 2026-09-09 确认：原两秒改为1.2秒 |
| 回复风格 | 简短回答、自然短反问；三句/每句五十字不再是硬限制 | S3-A8 |
| 断网 | 不超过五秒且原会话有效时同场；超过五秒重连是新场 | S3-A3 |
| 退出/刷新 | 新通话，不恢复前场聊天 | S2-6；S1-D024 |
| 空闲 | AI 播放完成、下一轮监听后，120 秒无人声，追加 10 秒倒计时 | S3-A5 |
| 取消空闲挂断 | 有效讲话或点击继续通话；普通点击无效；静音不暂停 | S3-A5 |
| 单次最长 | 15 分钟，提前 10 秒提醒 | S1-D060、D061，尚未被撤销 |
| 情绪和动作 | 不要求语义情绪表情，不做指定动作；保留自然微动 | S2-3、8；S1-D029 |
| 视频故障 | 黑屏、明确模式提示、继续纯语音；不播放双份声音 | S3-B12；同步实现约束 |
| 日志 | 必要运行元数据保留 30 天 | S3-B10 |
| 费用 | 测试期不做预算上限、费用预警或超额停话；仍记录用量 | S2-5；S1-D075、D076 |

**特别说明：** 取消每日次数与总人数，不等于取消单次 15 分钟。每账号五个入口因 U01 未冻结；本版不以它拒绝用户，亦不据此重新启用多对多业务。

## 04 业务对象与角色关系

### 4.1 统一词义

| 名称 | 含义 | 避免的混淆 |
|---|---|---|
| 角色资源 | 一份照片对应的数字人外观、平台人物编号及制作状态 | 不是一段提前录好的回答视频 |
| 用户专属配置 | 此资源面对某个账号时使用的身份、背景、称呼、口吻 | 不是另造一份外观，也不是共用聊天记忆 |
| 角色列表项 | 已完成且授权给当前用户的通话入口 | 不是永久公开的第三方播放链接 |
| 通话会话 | 一次连续交互，有独立身份、上下文和平台运行任务 | 不等同于长期保存的角色资料 |
| 一轮对话 | 用户发言结束到 AI 本轮实际播放完毕 | 不等同于整场通话 |
| 角色占用 | 当前共享资源有活跃或待清理的通话任务 | 不等同于系统人物总数或账号总数 |

### 4.2 已确认的共享方式

示例：资源 R 为同一个数字人外观。用户甲进入时，使用甲对应的背景、称呼和口吻；用户乙进入时，使用乙对应的配置。甲、乙不共享聊天上下文，且不能同时占用 R。

独占键必须是共享的 `avatar_resource_id`，不能是用户配置 ID。否则为每个用户创建一份配置就能绕过同一角色独占，与已确认规则冲突。

### 4.3 多对一与列表数量的处理

最新 B2 已明确多个用户可归属一个资源，不做任意多对多配置。数据模型先按“每个用户一份有效资源配置”的多对一方案说明，标记为待 U01 最终确认；不据此声称用户已最终撤销所有五个入口语义。

用户端保留列表形式，即使当前只有一个条目。列表容器并不自动证明一个账号可以配置五个不同资源；具体样式稍后确定。

### 4.4 制作计数规则

已确定：处理中记录占一个；明确失败记录不占；同一照片被用户明确再次用于新建，也形成独立的一次创建并计一个。照片内容相同不自动合并业务记录。

目前系统总量上限已取消，因此这些数字首先作为制作状态统计，不自行发明一个新的配额。它们未来作用于哪个账号、角色或制作额度，仍属于 U01。

技术重复请求与业务重复创建应分开：同一次提交因双击、刷新重试产生的网络重复，不应产生两次收费制作；用户明确点击“重新新建”则是另一份任务。实现上使用提交请求标识区分，不按照片哈希强行去重。

## 05 页面与用户流程

### 5.1 页面清单

| 页面 | 必要内容和操作 | 关键空态/异常 |
|---|---|---|
| P01 管理员登录 | 账号、密码、登录 | 错误提示，不提供短信或公开注册 |
| P02 用户登录 | 管理员发放的账号、密码 | 停用、密码错误、登录过期；提示联系管理员 |
| P03 管理员角色列表 | 名称、缩略图、处理中/可用/失败、启停、占用状态 | 空列表、制作失败、正在使用、待清理 |
| P04 创建角色 | 照片、基础名称/资料、提交 | 素材不合格、提交中、防重复提交 |
| P05 角色详情 | 状态、关联用户、专属配置、测试入口、异常处理 | 忙碌时只读；失败可重试或异常兜底 |
| P06 账号管理 | 创建账号、设置/重置密码、启停、查看授权关系 | 不展示原密码；重名、重置失败 |
| P07 用户配置 | 在角色下配置用户的身份、背景、口吻和称呼 | 角色忙碌禁止保存；绑定基数待 U01 |
| P08 我的角色 | 当前账号获准访问的已完成列表项 | 暂无角色、入口已停用、角色占用 |
| P09 通话页 | 数字人区域、状态、麦克风、挂断、计时、倒计时继续按钮 | 连接失败、短断恢复、黑屏纯语音 |
| P10 用量记录 | 时间、账号、角色、测试/用户会话、时长、错误和用量 | 费用不可取得时显示未知，不写 0 元 |

列表视觉样式、配色、卡片或表格形式未定，不作为阻塞功能开发的前置条件。管理员后台以电脑使用为主；用户端以电脑 Chrome 主线验证，其他适配从简。

### 5.2 管理员首次准备流程

1. 管理员使用预先初始化的账号登录。
2. 上传照片并填写角色基础资料，提交后得到任务记录。
3. 系统自动驱动人物创建；管理员可离开页面，后台仍跟踪任务。
4. 完成后自动进入可用状态，无需人工点击“制作完成”作为正常流程。
5. 管理员创建用户账号、设置密码，在角色下配置用户身份与口吻并授权。
6. 管理员可在角色空闲时点击测试。测试使用独立上下文，不进入用户正在使用的会话。
7. 用户登录后出现对应的可通话列表项。

绑定是否允许在处理中先配置，属于实现建议：可先保存，但完成前不允许通话；平台完成后自动使已授权入口生效，不增加人工发布审核。

### 5.3 用户通话流程

用户登录 → 获取自己的列表 → 点击角色 → 后端鉴权和独占检查 → 请求麦克风/准备播放 → 建立语音与数字人输出 → 正常通话 → 挂断或超时 → 清理任务与占用 → 返回列表或结束态。

进入列表不占用通话资源。点击入口后的准备阶段需要有占用预约，避免两人同时进入；该预约失败或超时必须释放。具体预连接顺序属于开发实现，不改变“每角色一次一人”。

### 5.4 建议统一提示文案

| 情况 | 页面提示 |
|---|---|
| 无可用角色 | 暂无可通话角色，请联系管理员。 |
| 制作中 | 人物正在处理中，完成后即可通话。 |
| 制作失败 | 人物创建失败，请查看原因并重新提交。 |
| 角色被占用 | 该角色正在通话，请稍后再试。 |
| 账号已有通话 | 该账号已有一场通话，请先结束原通话。 |
| 管理员修改忙碌角色 | 当前有人正在与该角色通话，请等待空闲后修改。 |
| 短断 | 连接暂时中断，正在尝试恢复。 |
| 超五秒后的新场 | 已重新建立通话，本次不会保留上一场聊天内容。 |
| 空闲倒计时 | 长时间未通话，即将在 10 秒后挂断。 |
| 画面失败 | 画面暂不可用，已切换为纯语音通话。 |
| 技术资源不足 | 当前通话服务暂时繁忙，请稍后再试。 |

文案为实现建议，可调整措辞，不改变规则。黑屏不是无解释的故障页面：状态和挂断、静音控件仍须可见。

## 06 账号、登录与权限

### 6.1 AUTH-01 账号创建与持久化【已定】

管理员统一创建和发放账号；用户名、状态、密码校验数据持久保存。普通用户不创建其他账号、不注册、不通过短信重置。

用户名唯一；账号停用与删除不同。Demo 必须支持停用，不因本 PRD 自动增加用户自助注销、批量导入或永久删除界面。

### 6.2 AUTH-02 密码管理【已定＋实现建议】

数据库只保存密码哈希校验值，不保存可查询的原密码。管理员在创建/重置时设置新密码并发放，后续不能查看旧密码。页面和日志不回显已有密码；请求日志对密码字段做剔除。

具体密码哈希库、密码强度、登录失败频控及登录有效期由技术方案确定，不在未询问时写死成额外产品门槛。需要具备基本防护，不能把不限制账号数理解为不限制恶意请求。

### 6.3 AUTH-03 多设备和会话撤销【已定】

同一账号可同时在手机和电脑登录，但一次只能创建一场进行中通话。第二设备得到“账号已有通话”，不能抢占。

管理员重置密码后，该账号旧登录凭证失效，要求重新登录。对依赖该失效登录的进行中通话应终止，不能仅让下一次 HTTP 请求失败而保留实时连接继续使用。

停用账号、停用入口、解绑用户后，受影响用户不能再进入；正在进行的受影响会话需终止。安全撤权与角色忙碌禁止修改的顺序见 U02，不能悄悄省略任一要求。

### 6.4 AUTH-04 权限矩阵【已定】

| 操作 | 管理员 | 普通用户 |
|---|---|---|
| 查询全部角色与制作状态 | 允许 | 不允许 |
| 创建人物、账号、配置用户口吻 | 允许，受忙碌保护 | 不允许 |
| 查询自己的可通话列表 | 可测试管理范围内资源 | 仅授权资源 |
| 启动通话 | 独立测试，不抢占用户 | 仅本人授权且角色空闲 |
| 查看他人聊天或加入他人通话 | 不提供此功能 | 不允许 |
| 查看用量元数据 | 允许 | 本版不提供完整历史管理 |
| 查询原密码、长期平台密钥 | 不提供 | 不提供 |

所有入口、资源详情、短期通话凭证、恢复请求均须后端重新鉴权。分享业务 URL 不授予访问权。用户修改请求里的账号 ID、角色 ID 或会话 ID，不能访问别人的资源。

### 6.5 AUTH-05 实现要点【实现建议】

账号记录保存认证版本 `auth_version`。登录票据携带或关联版本；重置/停用时提高版本，撤销旧登录并向活跃连接发送结束事件。实时通道也检查版本，不只保护网页请求。

Cookie/短期令牌采用既有安全实现；敏感请求防跨站滥用。平台短期播放凭证可以按 SDK 需要发给授权客户端，但长期密钥、可跨用户使用的管理员凭证不下发。

## 07 人物自动创建与素材管理

### 7.1 AV-01 素材与准入【沿用】

上传照片须满足最终供应商对应产品的清晰度、取景、角度、遮挡与格式要求。当前没有已确认的像素和大小数值，配置项须在接入验证后填写，不把旧介绍中的某一尺寸当作通用要求。

不合格照片直接提示重交。本版不做旧照片修复、AI 补脸/补手、批量美化，也不默默裁切改变人物内容。网页预览缩放不等于修改原始素材；若平台强制需要裁切或预处理，须记录差异并确认后使用。

### 7.2 AV-02 自动异步流程【已定】

| 阶段 | 行为 | 对外状态 |
|---|---|---|
| 受理 | 校验权限与素材，建立本地资源和制作任务 | 处理中 |
| 提交 | 服务端向供应商发起人物制作，记录关联标识 | 处理中 |
| 跟踪 | 后台查询状态或接收已验证来源的回调 | 处理中 |
| 成功 | 保存平台人物编号和可用信息，更新授权入口 | 可用 |
| 明确失败 | 保存经过脱敏的原因，允许重试/重交 | 失败 |
| 结果未知 | 提交超时但可能已成功，继续查证 | 处理中＋异常提示，不冒充明确失败 |

管理员关页面、刷新、重新登录，不影响后台任务跟踪。服务重启后依据持久任务记录恢复查询，不重复盲目创建。

### 7.3 AV-03 人工异常兜底【已定】

仅在正常自动路径出现无法自动处理的异常时，管理员/开发人员可联系平台处理，随后校验并绑定有效人物编号。记录失败原因、人工操作人、时间、来源为人工兜底。

验证时必须至少证明正常素材走全自动链路成功。人工创建成功、Mock 回调或直接填入公共人物编号，均不能算 AV-02 已通过。

### 7.4 AV-04 重复、重试和费用风险【已定＋实现建议】

用户明确再次用同一张照片新建时，允许产生不同的本地资源记录并计数；不以文件相同自动取消此次意图。同一创建请求的技术重传使用相同幂等键，只返回同一任务，避免误付两次。

失败任务重试需要有明确按钮和任务关联，保留失败历史但不重复计算同一在制资源。未知结果先查询原任务，确认未创建后才允许重新提交。禁止无法得知结果时持续自动新建。

本期没有费用上限和预算拦截，但有限重试、异常可见、用量留痕仍是基础可靠性要求，不是预算产品功能。

### 7.5 AV-05 可用、启停与占用分离【实现建议】

制作状态 `PROCESSING/READY/FAILED`、管理启用状态 `enabled`、运行占用状态 `IDLE/RESERVED/ACTIVE/CLEANUP_PENDING` 分开存储。人物已经制作成功但被停用，不应改成“制作失败”；角色忙碌也不应改掉制作状态。

在正常 UI 中只对授权且可用、启用的角色开放通话。失败素材不通过“纯语音降级”绕开人物未创建的问题，除非后续另行决定新增纯语音角色产品。

## 08 用户配置、绑定与编辑保护

### 8.1 CFG-01 按用户设置人物身份【已定】

共享外观资源与用户专属的角色名、背景、关系称呼、口吻分开。进入通话时由服务端根据当前账号读取正确配置，不接受客户端任意指定另一个用户的配置。

无长期聊天记忆不等于无角色资料。用户重新进入时仍使用管理员配置的人设，但不携带上一场互动内容。

### 8.2 CFG-02 通话中禁止修改【已定】

同一资源有预约建立、活跃通话、五秒短断保留或待清理任务时，一般内容修改不生效。管理员可查看，但编辑保存、替换照片、变更用户背景或口吻、替换平台编号等需提示等待空闲。

后端必须在保存时再判断，不仅进入页面时判断。实现建议：编辑保存和通话占用使用同一事务/互斥边界，防止管理员看到空闲后用户恰好开始通话，导致并发修改穿过检查。

本条覆盖与该资源有关的全部用户配置，不只是当前通话用户那一份。后台按钮禁用只是体验，服务端拒绝才是规则执行。

### 8.3 CFG-03 停用与解绑的冲突处理【待确认 U02】

已经确认撤销访问要结束受影响会话；同时确认忙碌时不能改角色。建议安全撤权使用单独流程：先阻止该目标继续授权/恢复，结束受影响通话并回收资源，再提交解绑或停用变更。普通改文案不能借此强行停止用户。

在操作顺序确认之前，不把“所有解绑都直接失败”或“通话中任意修改绑定”任一种写为最终验收。需要保留明确的操作提示和审计记录。

### 8.4 CFG-04 管理员测试【已定】

管理员测试占用同一资源，遵守不抢占规则。创建 `purpose=ADMIN_PREVIEW` 的独立会话，不复用用户聊天上下文、令牌和房间。

可用状态自动生效，管理员测试不是每个人物必须通过的人工发布审核。测试可选择已存人设配置来检查效果，但不读取用户正在进行或已经结束的聊天。

管理员测试是否使用默认人设还是可选择用户配置，是页面实现建议；无论选择何者，都须在界面标明当前测试配置，日志只记录配置版本，不保存聊天正文。

## 09 实时通话与状态流转

### 9.1 CALL-01 建立通话【已定＋实现建议】

1. 客户端提交所选角色资源及一次性请求标识，不自行指定用户身份。
2. 后端验证登录、认证版本、角色启用/可用状态及当前账号授权。
3. 原子检查并占用账号和角色。任一已被占用则失败，不先创建付费平台任务再检查。
4. 固化本次使用的配置版本，在运行内存中构建人设；创建独立的本地通话 ID 和平台会话。
5. 建立豆包语音会话及数字人运行任务、前端播放连接。每一步失败都回收已创建部分。
6. 播放链路与麦克风准备好后进入可通话状态；单次时长由此开始计算。
7. 符合视频故障降级条件时进入黑屏纯语音，否则向用户显示连接错误并清理。

重复点击同一次“开始”不得建立多场。开始请求返回前页面保持连接中；用户可随时取消，迟到的成功回调不得重新打开已取消的通话。

### 9.2 CALL-02 发言轮次【沿用＋已定】

进入监听后接收用户声音。只有检测到用户本轮确实开始讲话，才启用1.2 秒静音判停；空房间1.2 秒安静不触发模型自行回答。讲话后不足1.2 秒的停顿仍属于当前发言，恢复人声则取消判停等待。

确认发言结束后切换为思考/回复。生成回复和播放期间不提交新的用户发言，不支持语音插话；用户仍可静音、挂断。声音实际播放完毕后重新监听，不能把“豆包输出结束”或“向数字人上传结束”当作播放结束。

S3-A8 的风格要求：像正常语音聊天，简短回答，必要时一句自然反问；不强制每句话都反问，不使用“三句话、每句五十字”的机械截断作为验收。模型输出长度是风格评估，不因多一个字强行切断语音。

### 9.3 CALL-03 正交状态模型【实现建议】

为避免把静音、无视频、重连混成几十种互斥状态，建议分为三个状态维度，另存静音开关：

| 维度 | 状态 | 含义 |
|---|---|---|
| 生命周期 | CONNECTING / ACTIVE / RECONNECTING / ENDING / ENDED | 通话是否存在、是否正在清理 |
| 对话轮次 | LISTENING / CAPTURING / WAITING_REPLY / PLAYING / IDLE_WARNING | 当前由谁讲话及空闲提醒 |
| 输出模式 | VIDEO / AUDIO_ONLY | 有数字人画面或黑屏纯语音 |
| 输入开关 | mic_muted=true/false | 用户静音，不等于通话结束或空闲暂停 |

只有 ACTIVE 才执行正常对话轮次；RECONNECTING 时不继续盲目投递旧音频。ENDING 后拒绝新录音、恢复和继续通话操作。ENDED 不再回到 ACTIVE；超过五秒创建的新场使用另一个通话 ID。

### 9.4 CALL-04 关键事件转换【实现建议】

| 当前状态与事件 | 后续行为 |
|---|---|
| LISTENING 检测到有效人声 | 进入 CAPTURING，更新最后人声时间 |
| CAPTURING 连续1.2 秒无声 | 提交本轮结束一次，进入 WAITING_REPLY |
| WAITING_REPLY 收到可播放音频 | 由当前输出路径播放，进入 PLAYING |
| PLAYING 最后一段实际播放完成 | 清空本轮待播放状态，进入 LISTENING，启动新空闲计时 |
| LISTENING/CAPTURING 阶段出现空闲警告条件 | 进入 IDLE_WARNING，显示十秒倒计时 |
| IDLE_WARNING 人声/继续通话 | 取消倒计时，重新计算空闲；有声时继续收集发言 |
| 任意活跃阶段点击挂断或授权撤销 | 进入 ENDING；取消计时、停止输入输出并清理 |
| 网络短断 | 进入 RECONNECTING，按五秒规则处理 |
| 视频路径失败且语音有效 | 输出模式切 AUDIO_ONLY，不因此自动重置聊天上下文 |

“继续通话”不自动解除用户主动静音，也不会制造一轮空白发言发给豆包。

### 9.5 CALL-05 同时到达的事件优先级【实现建议】

主动挂断/授权失效优先于继续通话、恢复、媒体回调；到达单次最长时长优先于空闲重置；已结束的旧场事件永远不能恢复新场。对于恰好在倒计时结束时讲话，按服务端接受的有效事件时间判定，保证结果可重现，不依赖哪个按钮动画先刷新。

所有结束请求均可重复安全调用。统一记录结束原因，例如 USER_HANGUP、MAX_DURATION、IDLE_TIMEOUT、NETWORK_TIMEOUT、AUTH_REVOKED、SERVICE_ERROR；不得重复累计通话时长。

## 10 计时、记忆与五秒重连

### 10.1 TIME-01 三类时间不可混用

| 计时 | 起点 | 触发和结束 |
|---|---|---|
| 本轮1.2 秒静音判停 | 本轮已有人声后的最后有效声音 | 连续1.2 秒无人声，提交本轮发言结束 |
| 空闲 120＋10 秒 | AI 实际播放结束、进入下一轮监听 | 连续 120 秒无人声提醒，再十秒无人声且未点继续则挂断 |
| 整场 15 分钟 | 成功进入可通话状态 | 第 890 秒提示，900 秒结束；不因讲话、静音或继续按钮延长 |

首次连接后还没有过 AI 回复的第一次监听，用户没有单独定义空闲起点。实现建议同样从首次 LISTENING 开始 120＋10 秒，防止从未讲话的会话永久占用；该默认值列于 U05，不伪装成已补充确认。

### 10.2 TIME-02 空闲规则【已定】

“120 秒无人声”由麦克风检测信号决定，不依靠用户发出文字、点击空白或页面是否可见来判断。实现使用已有的人声/有效声音检测结果；阈值和噪声容忍需要实测，不把每个非零采样或键盘点击直接等同于有效讲话。

正常示例：AI 于 t=0 播放结束并恢复监听；t=0—120 秒持续无人声；t=120 秒显示“即将在 10 秒后挂断”；t=130 秒仍无人声且未点击继续，结束通话。

在 t=125 秒用户讲话或点击继续，倒计时取消，空闲从此次有效交互后重新计。普通点击、鼠标移动、网络心跳、数字人眨眼、系统自身提示都不能重置。麦克风主动静音也不暂停，静音用户可以点击继续延长等待。

AI 正在思考或播放时不计这项空闲，但必须有独立的服务异常超时。不能因为 WAITING_REPLY 状态排除了空闲，就让平台不返回时永远占用资源。

### 10.3 TIME-03 单次时长【沿用】

当前保留单次 15 分钟和提前十秒提示，因为后续 A2 讨论的是账号总量、人物总量、固定并发和每日次数，未明确撤销 D060/D061。

实现建议从后端确认可通话时起计；前端显示剩余时间但不是最终裁判。五秒内短断和纯语音降级都仍属同场，不能重新计满 15 分钟。超过五秒建立的新场重新起计；目前没有每日次数/总分钟上限，不另外阻止合法重进。

### 10.4 MEM-01 三种数据生命周期【已定】

| 数据 | 保留方式 |
|---|---|
| 账号、照片、人物编号、角色基础资料、用户专属口吻 | 持久保存，用于以后登录和新通话 |
| 本场聊天上下文、临时输入输出音频、播放进度 | 仅运行期间必要保留；新场不继承，不写成聊天历史 |
| 通话时间、时长、状态、任务编号、错误、用量 | 必要元数据保存 30 天 |

单次上下文持续是产品目标，不是无限上下文长度的保证。实际模型上下文上限和长通话行为属于供应商验证；遇到失效不能悄悄新建上游会话却继续宣称记忆完整。

### 10.5 NET-01 五秒边界【已定】

| 情况 | 业务结果 |
|---|---|
| 连接中断持续不超过 5 秒，原页面和服务端会话仍有效 | 继续同场，本地通话 ID 和聊天上下文不变 |
| 持续超过 5 秒才恢复 | 旧场结束；重连须创建新的通话和上游语音会话，不注入旧聊天 |
| 刷新、退出重进、主动挂断后再进入 | 无论间隔是否小于 5 秒，都是新场 |
| 不足 5 秒但上游会话已不可恢复 | 不冒充同场恢复成功；作为服务异常，提示重新建立新通话，列为能力验证边界 |
| 只丢数字人画面、语音链路仍正常 | 按视频故障降级处理，不机械地把它当整场网络重连 |

这里的五秒指检测到连接失联到确认恢复的持续时间，不是“通话总共只讲了五秒”。检测起点与实际物理断网可能有偏差，需要在测试中记录。建议由后端产生一次失联截止时间并使用单调时钟判断，客户端不能自己宣称“尚未超时”。

恰好 5.000 秒按“不超过”处理，5.001 秒按新场处理；工程事件精度和检测延迟须由测试时钟统一验证。

### 10.6 NET-02 旧场结束和新场建立【实现建议】

超过五秒后，旧场进入 ENDING，取消恢复资格，关闭/补偿清理平台任务，撤销旧媒体凭证并丢弃临时上下文。网络恢复后重新验证账号、角色授权与角色空闲，再创建新场。

新场不是无条件抢回资源。如果旧任务仍处于清理状态，或其他合法用户已获得角色，必须提示等待/占用，不能为了“自动重连”违反角色独占。清理完成且角色可用时才建立新的通话。

建议每场使用 `session_id + generation` 标识。旧场音频、成功回调、停止回执、迟到心跳不能归入新场；旧清理任务也不能按单一角色 ID 删除新场占用。

### 10.7 NET-03 短断时媒体和计时【实现建议 / 待验证】

五秒内恢复需保留已成立的上下文，但不保证把用户没上传成功的半句话、没听到的半句回复无损重播。待确认的实现策略是：丢弃未提交的残缺输入，恢复有效会话；输出根据已知播放边界恢复，无法定位时明确中止当前回复，不从头重复播放。

短断不能被当作用户互动来清空空闲时间，也不能延长本场最大时长。恢复时同步剩余计时。整场结束或新场建立才重置会话时钟。

后台/锁屏不作稳定通话承诺，也不增加专门后台续聊功能；仍应依现有连接事件执行清理，不能把不承诺后台体验解释为允许长期残留任务。

## 11 音视频同步与纯语音降级

### 11.1 MEDIA-01 统一输出原则【实现约束】

豆包是回复内容和声音的来源。视频模式下，声音交给数字人服务并按照该接入模式的匹配音视频输出播放；原纯语音播放器不得同时先播一遍。

若实际 SDK 返回的音频、画面采用不同轨道，仍需以同一会话的匹配时间信息调度；不能靠“两个播放器同时点开始”推定同步。具体是否返回声音、能否获得播放进度，必须实测，不把内部开发事件当成供应商已有能力。

输入音频格式、分片长度、采样率和声道由实际供应商契约决定。本文不沿用未经复核的 16k/24k 或某个具体协议字段。发生重采样时只做格式适配，不重新生成另一种音色。

### 11.2 MEDIA-02 输出归属【实现建议】

本地维护 `output_owner=AVATAR/DIRECT/NONE`。视频正常时 AVATAR 唯一发声；降级后 DIRECT 唯一发声；结束时 NONE。任何时刻不能存在两条可听输出。

每轮音频使用 reply_id、递增片段编号和可计算的采样范围跟踪。接收完成、送达平台、生成完成、播放完成是不同事件；只有最后一个可用于恢复监听。原始音频仅在必要的内存缓冲中保存，不能为了降级把每场录音长期落盘。

### 11.3 MEDIA-03 黑屏纯语音【已定】

数字人出错时，画面区域变为黑屏/无画面，保留状态、静音和挂断控件，显示已切换纯语音。只要豆包语音与用户连接仍有效，就继续本场对话；不清空上下文，不释放仍在使用的角色业务占用，不重置 15 分钟。

角色忙碌、账号已有通话、无授权、人物未创建成功，不是可以靠降级绕过的条件。语音服务本身不可用也不能靠黑屏继续正常通话。

视频故障本身不要求用户确认后才能降级；这是 S3-B12 对旧版“不降级”的明确覆盖。是否在同场自动恢复视频未被决定，实现建议本场保持纯语音，下一场重新尝试视频，以减少反复切换。

### 11.4 MEDIA-04 正在说话时如何切换【待技术验证】

直接把整段豆包回复从头播放会重复用户已听过的内容，不能作为合格兜底。推荐过程：先停止/静音旧输出，清理其可播放队列，再从可靠的已播放边界衔接剩余声音，确认唯一输出后继续。

“已收到/已上传多少音频”不能证明“用户已经听到多少”。若供应商无法提供可用播放反馈，应验证替代的统一音频路径。不能承诺尚未验证的无缝、无丢字接力。

最低降级目标是：不出现双声和整段重复；本场语音链路可继续、下一轮可正常回答。若本轮进度未知而无法安全接力，建议提示切换并中止本轮剩余播放，再恢复下一轮监听；此暂定策略和上游上下文一致性需通过 G03，不能隐瞒成无损降级。

### 11.5 MEDIA-05 视觉质量【已定＋待样本验收】

正脸、肩部以上；嘴和面部随音频自然变化，允许眨眼与轻微转头。无需指定动作、手臂、精确情绪变化或用户话语驱动的倾听情绪。

用客户实际照片与固定测试音频确定观感基准，检查人脸身份、牙齿、皱纹、嘴角、头部微动、切换闪烁、首尾同步。数值延迟与帧率尚未约定，不在本版凭空写成 200ms 或 30fps 的保证。

黑屏降级是异常容错，通过降级测试不代表数字人画面质量或视频功能已验收成功。

## 12 容量、异常与资源回收

### 12.1 CAP-01 不设产品总量上限【已定】

不再按 3 个账号、5 个系统人物、固定 2 场通话、每日 3 次拒绝请求；不提供这些旧额度的排队或次数重置逻辑。每日 10 人不作为本次强制容量目标。

账号、人物和制作记录可以继续增加，但不能把它描述为无限免费存储或无限同时运行。费用预算等主线通过后再评估，不因此取消运行记录。

### 12.2 CAP-02 技术资源不足【实现建议，未另立商业额度】

本场能否建立取决于后端处理能力、语音服务、数字人服务等实际可用性。技术连接保护、请求限速和已知供应商配额与商业“每天只能聊三次”不同。

当前不制作复杂容量后台或排队系统。资源不足时返回明确错误，不无限等待。若只缺视频而语音可用，可按已确认降级目标处理；是否覆盖首次视频启动失败应作为 G03 测试场景，不把授权或角色独占错误归入此类。

不硬编码某个供应商并发值；配置来源为实际账号测试/合同。没有确认额度时显示未知，不能显示“无限”。

### 12.3 ERR-01 故障分类与动作

| 故障 | 产品行为 | 必须清理/保留 |
|---|---|---|
| 登录失效、未授权 | 拒绝访问或结束受影响通话 | 撤销令牌和会话，不返回他人资料 |
| 角色忙碌、账号已有通话 | 提示稍后或先结束旧通话，不排队 | 不创建新的平台任务 |
| 麦克风权限拒绝 | 明确提示，不能假装监听成功 | 释放已预约的角色 |
| 人物创建失败 | 显示失败与重交/重试入口 | 保留任务元数据，失败不计制作额度 |
| 数字人视频启动/运行失败 | 在语音有效前提下黑屏纯语音 | 释放无用视频任务，保留本场语音和角色占用 |
| 豆包无响应/连接失效 | 按服务超时策略报错或结束 | 不用 120 秒空闲掩盖服务卡死 |
| 网络不超过五秒 | 尝试保留同场恢复 | 保留上下文和独占，防第二人占入 |
| 网络超过五秒 | 旧场结束，恢复后按新场建立 | 旧媒体及上下文作废，重新鉴权 |
| 服务停止回执失败 | 前端结束；后台补偿清理 | 标记 CLEANUP_PENDING，不伪装已完全释放 |
| 管理员重置/停用/解绑 | 旧登录无效或终止受影响通话 | 按 U02 明确撤权顺序 |

### 12.4 CLEAN-01 结束必须执行【已定＋实现建议】

停止接收用户音频 → 取消所有判停/空闲/服务计时 → 停止唯一音频输出并使画面结束 → 结束豆包会话 → 停止数字人运行任务 → 关闭实时通道 → 清除临时上下文和缓冲 → 更新结束原因、时长和用量 → 释放账号和角色占用。

顺序可按 SDK 特性调整，但每一项必须可重复执行、可补偿。浏览器 unload 只能是辅助信号，不能是唯一清理触发。服务端失联检测和任务核对必须覆盖浏览器直接关闭或后端进程异常。

旧任务清理未确认时，不允许把可能仍在运行的同一平台人物立即分给另一人。清理重试不能按角色 ID 无条件关闭所有任务，必须指定旧场任务及代次，防止误杀新场。

### 12.5 ERR-02 服务异常超时【已定需求，数值待配置】

需要区分启动超时、首段语音超时、回复流中断、数字人首画面/视频输出超时、任务停止超时。它们不使用统一的 120 秒用户空闲计时替代。

本版没有客户已确认的秒数。开发采用命名配置并在接入测试中选取，不可将临时测试值写成对客户的性能保证。任务制作长期处理中也要能告警/标记异常和查证，但不擅自新增固定制作完成时间承诺。

## 13 开发架构与模块职责

**本节为实现建议，目的在于把已定需求变成可开发模块，不额外采购新平台或重写已完成的语音闭环。**

### 13.1 逻辑架构

```tex
管理员网页 / 用户网页
        |
        v
业务后端：认证、角色、配置、权限、会话、用量
        |                         |
        v                         v
持久数据：账号/素材/任务/元数据   人物创建后台任务
        |                         |
        v                         v
通话控制器                 数字人供应商创建接口（待验证）
        |
        +--> 豆包语音适配器 --> 回复音频
        |                         |
        |                         v
        +--> 数字人驱动适配器 --> 匹配音视频 --> 网页
                                  |
                            失败时唯一音频路径切换
                                  v
                         网页直接播放豆包声音
```

具体媒体是否直接从供应商到浏览器、是否通过供应商 SDK 推送音频，取决于真实接入模式。示意图表示职责，不意味着所有视频都要经过业务后端，也不假定仅替换一个 URL 就可换平台。

### 13.2 模块责任

| 模块 | 负责 | 不负责 |
|---|---|---|
| 认证模块 | 管理员发账号、登录、认证版本、撤销 | 短信、自助注册、营销用户体系 |
| 角色管理 | 素材、状态、可用入口、启停 | 实时人脸模型训练与手工逐帧动画 |
| 用户配置 | 用户对应的身份、背景、称呼、口吻 | 跨通话学习和历史记忆召回 |
| 制作任务执行器 | 提交、查询、重试、异常兜底留痕 | 用固定 sleep 模拟平台成功 |
| 通话控制器 | 鉴权、独占、轮次、计时、重连、清理 | 直接暴露平台密钥给页面 |
| 豆包适配器 | 沿用现有语音链路并暴露统一事件 | 擅自更换音色或引入另外一套聊天模型 |
| 数字人适配器 | 创建/驱动/终止、状态和媒体反馈 | 承诺平台没有验证的情绪或指定动作 |
| 输出协调器 | 唯一音频出口、播放结束、黑屏降级 | 把上传完成当作听完 |
| 运行记录 | 30 天元数据、次数/时长/用量 | 保存原始聊天、录音录像 |

### 13.3 供应商适配能力契约【内部抽象，非真实 API 名称】

| 能力 | 建议内部方法/事件 | 必须验证 |
|---|---|---|
| 从素材创建人物 | submitAvatarCreation / queryAvatarCreation | 正常路径能全自动获得最终人物编号 |
| 启动既有人物通话 | startAvatarSession | 与创建新人物是两种操作，不可混淆 |
| 持续输入既有语音 | pushReplyAudio / finishReply | 不要求换掉豆包，也不必等待整段录音才工作 |
| 媒体状态反馈 | mediaReady / mediaFailure / playbackProgress | 哪些是平台提供，哪些需客户端补充 |
| 一轮播放结束 | replyPlaybackCompleted | 不能仅依赖生成完成回执 |
| 终止与资源查询 | stopAvatarSession / inspectTask | 是否实际结束、如何查证遗留任务 |
| 五秒内恢复 | reconnectTransport | 原会话能否保留，不承诺平台自动支持 |

所有供应商字段、地址、鉴权和事件编号统一放在适配层，根据已取得的正式文档及测试账号实现。现有资料不足以证明上述能力全部已开放，因此接入验证 G01—G04 先于声称主线已完成。

### 13.4 最小工程拆分建议

复用现有前后端、音频采集和播放模块；新增账号持久化、角色/配置、人物制作后台任务、数字人适配与通话管理。不要求引入微服务、消息总线或分布式部署。

异步人物制作可以先由单个后台执行器完成；任务状态必须持久化。并发独占使用有原子保证的数据库事务或共享锁设施，不能只靠某个网页变量。

业务后端不负责数字人 GPU 推理。本版不包含自部署 LiveTalking、MuseTalk 或 Live2D 的新开发范围；平台不能满足时需要另行确认路线，而非偷偷更换为另一套产品。

## 14 建议数据模型

**以下字段是开发建议，不是已经选定的数据库产品和 SQL。U01 会影响用户配置表的唯一性约束，迁移前必须确认。**

### 14.1 `users`：管理员和用户

| 字段 | 用途与规则 |
|---|---|
| id、username | 系统 ID、唯一登录账号；不用手机号充当必填标识 |
| user_type | ADMIN / USER；首版一个管理员，无多级组织 |
| password_hash | 专用密码校验值，不保存原密码 |
| status、auth_version | 启用/停用、用于撤销旧登录的版本 |
| created_at、updated_at | 基础管理时间 |

### 14.2 `auth_sessions`：多设备登录

| 字段 | 用途与规则 |
|---|---|
| id、user_id、auth_version | 与用户和认证版本关联 |
| token_hash / server_session_ref | 取决于现有认证方案，不存可公开使用的原令牌 |
| expires_at、revoked_at | 到期和撤销；具体有效时长待配置 |
| device_label、last_seen_at | 可选的必要设备信息，避免收集无用指纹 |

多条登录记录不等于允许多条通话。认证记录过期清理是技术生命周期，不与 30 天运行日志混用。

### 14.3 `avatar_resources`：共享人物资源

| 字段 | 用途与规则 |
|---|---|
| id、display_name | 本地资源 ID 与管理名称 |
| photo_object_key、photo_hash | 受控图片存储引用；hash 仅供校验，不强制业务去重 |
| provider、provider_avatar_id | 供应商及已成功创建人物编号 |
| creation_status、enabled | 制作中/可用/失败；管理启停独立 |
| default_profile、config_version | 可选基础人设；实际用户配置不从聊天中反向更新 |
| created_by、created_at、updated_at | 创建和管理元数据 |

### 14.4 `avatar_creation_jobs`：异步任务

| 字段 | 用途与规则 |
|---|---|
| id、resource_id、request_key | 本地任务与幂等提交标识 |
| provider_job_ref、attempt_no | 外部任务与尝试次数 |
| state、next_check_at、last_error_code | 提交/跟踪/异常状态，不把未知写成失败 |
| source_mode | AUTO / MANUAL_FALLBACK；人工有原因及操作人 |
| submitted_at、completed_at | 制作时间记录，不作为固定完成 SLA |

### 14.5 `user_role_profiles`：用户对应的人物配置

建议字段：user_id、avatar_resource_id、role_name、background、relationship、addressing、speaking_style、enabled、config_version、updated_by、updated_at。

按最新 B2 的多对一主线，建议每个 user_id 最多一份有效配置，多个 user_id 可以指向同一个 avatar_resource_id。此唯一约束待 U01 冻结；不直接建成默认可任意多选的多对多管理界面。

不把共享资源的默认人设与用户口吻强行混为一段；进入通话时按明确的覆盖规则合成运行配置。建议优先采用用户专属字段，其余继承资源默认值，保存时能预览最终效果。

### 14.6 `call_sessions`：每一场通话

| 字段 | 用途与规则 |
|---|---|
| id、generation、purpose | 唯一通话 ID、代次、USER / ADMIN_PREVIEW |
| actor_user_id、resource_id、profile_version | 调用者、独占资源、人设版本；不存聊天正文 |
| lifecycle、turn_state、media_mode | 运行状态；终态不可回退 |
| started_at、ended_at、end_reason | 计时起止与原因 |
| provider_voice_ref、provider_avatar_task_ref | 受控服务端关联；不当作公开访问令牌 |
| disconnected_at、reconnect_deadline | 五秒判断字段，不由客户端任意延长 |
| cleanup_state | 未清理/处理中/完成/待补偿 |

本场完整人设快照与临时上下文建议只在运行内存中保留；记录表仅留版本引用。不得为了排障把每轮 Prompt 和识别文字长期存进本表。

### 14.7 `resource_leases`：账号和角色独占

建议字段：scope（ACCOUNT / AVATAR）、scope_id、owner_session_id、owner_generation、state、lease_expires_at、last_heartbeat_at。

同一 scope＋scope_id 仅一个有效 owner。获取账号锁和角色锁须原子提交，失败回滚；续租和释放校验 owner 与 generation，旧场不能解开新场的锁。锁过期只是触发查证/清理，不是自动证明供应商任务已停止。

单实例实现可用数据库占用字段而非单独表，但必须满足同样竞态条件。不能依赖用户浏览器正常点击退出。

### 14.8 `usage_events` 与 `audit_events`：最小运行记录

保存发生时间、会话/任务关联、来源、事件类型、时长、计量单位、真实用量或可追溯估计值、结果码、操作人。运行与操作日志默认保留 30 天，清理策略见第 16 节。

不保存密码、访问令牌、原始照片字节、整段音频、完整用户发言、完整 AI 回复、完整人设文本。没有供应商费用数据时使用空值/未知，不用 0 元冒充免费。

### 14.9 数据一致性约束

账号唯一；平台回调按任务关联校验；相同提交标识仅一份创建；同一账号与角色只能一场活跃/待清理通话；通话开始和内容保存相互排斥；超过五秒新场有新上下文；重置密码旧会话不能继续授权；30 天清理不删除仍需使用的账号和角色业务资料。

## 15 业务接口与事件契约

**以下是自己后端的建议接口，不是第三方官方调用地址。可按现有项目框架改名，但必须保留权限、幂等、状态和错误语义。**

### 15.1 账号与人物管理接口

| 接口建议 | 作用 | 关键校验 |
|---|---|---|
| POST /api/auth/login | 账号密码登录 | 启用、密码、基本频控 |
| POST /api/auth/logout | 撤销当前登录 | 结束依赖当前登录的通话，操作幂等 |
| GET /api/me | 当前用户基本状态 | 不返回密码校验值或平台密钥 |
| POST /api/admin/users | 管理员创建账号 | 管理员权限、用户名唯一 |
| POST /api/admin/users/{id}/password-reset | 设置新密码 | 撤销旧登录和受影响连接；不记录密码 |
| PATCH /api/admin/users/{id}/status | 启停账号 | 按 A6 及 U02 处理进行中会话 |
| POST /api/admin/avatars | 上传素材并异步创建 | 管理员、素材、幂等键；返回任务状态，不等制作完 |
| GET /api/admin/avatars/{id} | 获取人物及制作状态 | 管理员权限，外部敏感编号受控 |
| POST /api/admin/avatars/{id}/retry | 明确失败后的重试 | 状态已明确、原任务查证、幂等 |
| PATCH /api/admin/avatars/{id} | 修改角色资料 | 后端检查角色空闲并避免竞态 |
| PUT /api/admin/users/{id}/role-profile | 设置用户资源配置 | 基数待 U01；忙碌保护；不默认支持数组多选 |
| GET /api/me/avatars | 用户可见的通话列表 | 服务端按本人授权过滤 |
| GET /api/admin/usage | 查看时长和运行用量 | 管理员；30 天范围，无聊天正文 |

人工兜底绑定编号建议提供受限管理操作，而非普通用户接口；只允许异常记录、需写操作原因并校验编号归属。

### 15.2 通话接口

| 接口建议 | 作用 | 关键校验 |
|---|---|---|
| POST /api/calls | 开始用户通话或管理员独立测试 | 登录、授权、角色可用、账号/角色原子独占 |
| GET /api/calls/{id} | 读取本人会话状态 | owner 与认证版本，不泄露其他会话 |
| POST /api/calls/{id}/resume | 不超过五秒恢复申请 | 原页面恢复票据、原场、截止时间、owner 均有效 |
| POST /api/calls/{id}/continue | 取消空闲倒计时 | 活跃且允许操作；不延长最大时长、不自动解静音 |
| POST /api/calls/{id}/end | 主动结束 | 本人或受控管理员操作；可重复安全调用 |
| 实时业务通道 | 麦克风片段、状态、恢复通知 | 与会话、账号、代次绑定；无公开房间万能票据 |

刷新页面不是调用 resume 继续旧聊天的理由。resume 凭证不设计为用户可以复制到另一设备永久续聊的令牌。

### 15.3 成功响应示例【内部格式建议】

```json
{
  "session_id": "call_example_01",
  "generation": 1,
  "lifecycle": "CONNECTING",
  "media_mode": "VIDEO",
  "role_resource_id": "avatar_example_01",
  "policy": {
    "speech_end_silence_ms": 1200,
    "reconnect_grace_ms": 5000,
    "idle_warning_after_ms": 120000,
    "idle_countdown_ms": 10000,
    "max_call_duration_ms": 900000
  }
}
```

示例不含真实平台密钥。业务通道地址和短期播放凭证应另行按供应商适配返回，凭证必须限定会话与权限。

### 15.4 业务事件

| 事件名建议 | 触发含义 |
|---|---|
| call.ready | 成功进入可通话状态，开始整场计时 |
| turn.listening / turn.generating / turn.playing | 轮次状态改变，前端显示与输入开关随之更新 |
| reply.audio.chunk | 音频片段或其路由元数据；严格带 session_id、generation、reply_id |
| reply.playback.completed | 最终输出播放完成，允许下一轮监听 |
| call.idle.warning | 空闲已到 120 秒，含服务端倒计时截止时间 |
| call.idle.cancelled | 人声或继续按钮使警告取消 |
| call.reconnecting / call.resumed | 五秒内恢复过程；不重置场次计时 |
| call.media.degraded | 切为黑屏纯语音，含故障分类、不含秘密字段 |
| call.ending / call.ended | 结束原因和清理结果 |
| auth.revoked | 登录撤销，立即停止继续使用 |

### 15.5 错误响应

| 业务错误 | 意义及建议 HTTP 状态 |
|---|---|
| AUTH_REQUIRED / AUTH_REVOKED | 需重新登录，401 |
| FORBIDDEN | 未授权，403；不透露其他角色敏感详情 |
| ROLE_BUSY / ACCOUNT_BUSY | 冲突，409；不排队、不创建重复资源 |
| ROLE_NOT_READY / ROLE_DISABLED | 不可通话，409 或约定业务状态 |
| ROLE_EDIT_BLOCKED | 通话中禁止修改，409 |
| SESSION_EXPIRED | 原会话不能恢复，409；引导建立新场 |
| CAPACITY_UNAVAILABLE | 必要服务暂不可用，503；不误写成角色占用 |
| INPUT_INVALID | 素材/输入不符合明确规则，400/422 |
| PROVIDER_FAILURE / SERVICE_TIMEOUT | 显示可理解提示，内部保留可追踪编号 |

错误返回建议统一包含 code、message、request_id、retryable。原始供应商堆栈、密钥、完整响应和敏感资料不直接显示给用户。

## 16 安全、日志与数据生命周期

### 16.1 SEC-01 基础保障【已定】

后端权限验证、用户隔离、长期密钥保留后端、通话结束任务清理、真实素材授权确认、AI 数字人身份说明、第三方数据流向告知、必要的安全访问和麦克风授权提示，均由 S1-D113—D120 保留。

本文不扩写法律结论，也不宣称平台自动接受逝者素材。上传真实客户资料前须确认适用平台的场景接受条件和授权材料；没有确认之前使用具备明确使用权限的测试素材。

### 16.2 DATA-01 保存与清理

| 数据类别 | 本次策略 | 清理注意点 |
|---|---|---|
| 用户、角色、图片与用户配置 | 持久保存以支持产品功能 | 不因日志 30 天自动删掉账号或人物 |
| 未完成人物制作任务 | 保存到结果可追踪，维护当前业务状态 | 任务异常日志 30 天；超长处理中不能因日志清理失去任务归属 |
| 运行日志、事件明细、已结束通话明细 | 默认 30 天 | 清理后台执行并记录数量，不要求新增用户删除界面 |
| 本场聊天上下文 | 内存或上游会话中临时存在 | 结束/新场不继承；不写持久聊天表 |
| 音频/视频片段 | 仅必要流式处理缓冲 | 不生成供回放的录音录像；结束后清理 |
| 平台侧数据 | 需单独核实 | 不能把本应用不保存，表述成所有第三方均不保存 |

建议按事件发生时间计算三十天明细保留期；汇总统计默认同样限定可查三十天，不自行增加永久个人行为统计。若制作故障元数据需长期留存，另行定义最小必要字段，而非保留所有原始日志。

### 16.3 OBS-01 必要指标【实现建议】

记录通话建立成功/失败、角色忙碌拒绝、视频降级、短断恢复、新场重建、各结束原因、创建成功/失败、清理待补偿数量、运行时长、已取得的供应商用量。

回复延迟在同一时钟域中分别测量：用户判停→后端首音频、前端可播放→实际播放；不要直接相减未校时的浏览器与服务器时间。服务端计时、媒体时间戳、客户端体验指标分别留字段。

必要运行告警（任务清理失败、服务不可用）不等于已删除的费用预算告警。告警渠道和阈值待技术配置，本版不增加短信通知采购。

### 16.4 COST-01 用量不是费用承诺【已定】

本次不启用 40 元/人物、10 元/十分钟、月度费用上限、费用预警、达到费用上限自动停话等旧规则。仍保存必要次数、时长和调用元数据，作为主线通过后的预算依据。

费用字段需要来源和估算标识；供应商未提供结算数据时显示“暂无/待核对”。不把接收音频分片数量当作生成次数或账单，不在 PRD 中写未经确认的即构套餐单价。

## 17 参数、部署与运行配置

### 17.1 已确认/沿用参数

| 参数建议名 | 值 | 来源 |
|---|---|---|
| speech_end_silence_ms | 1200 | S1-D011 |
| reconnect_grace_ms | 5000，边界包含五秒 | S3-A3 |
| idle_warning_after_ms | 120000 | S3-A5 |
| idle_countdown_ms | 10000 | S3-A5 |
| max_call_duration_ms | 900000 | S1-D060 沿用 |
| max_duration_warning_ms | 10000 | S1-D061 沿用 |
| runtime_log_retention_days | 30 | S3-B10 |
| account_total_limit / avatar_total_limit | 不启用产品总量限制 | S3-A2 |
| daily_call_limit / fixed_two_call_limit | 不启用 | S3-A2、B7 |
| cost_budget_limit / budget_alert | 不启用 | S2-5 |
| per_account_role_slots | 暂不冻结为五个；与关系模型一起确认 | U01 |

### 17.2 未确定的技术配置

服务启动、首语音、语音片段中断、视频首帧/停滞、任务清理、制作查询间隔、最长自动重试、失联检测间隔、登录票据有效期、有效人声阈值、照片大小与格式白名单均待接入验证。

这些配置需要有明确名称和默认值来源，测试环境可采用临时值，但必须在配置说明标为“测试初值”，不能混成客户已确认参数。有限重试和保护性限流不属于恢复已取消的人数/费用产品限制。

### 17.3 部署组成【实现建议】

一个 Web 前端、一个业务后端、持久账号/角色数据库、受控照片存储、后台人物制作/清理执行器，外接豆包和数字人服务。可先同机或同应用部署，不要求复杂集群。

服务端从部署环境读取密钥、数据库连接和平台产品配置；不写进源码、前端静态包或可公开日志。远程访问应准备能正常使用麦克风和播放的安全网页环境，测试必须覆盖真实访问地址而非只验证本机。

当前未选择具体云厂商、机型、数据库品牌、月度预算或配额，不提供凭空的采购标准。正式部署预算与扩容待主线通过后评估；部署验证先记录实际 CPU、内存、连接数、网络用量和供应商错误。

### 17.4 环境适配

主线：电脑 Chrome，验收时记录实际浏览器、操作系统及版本。管理员后台电脑优先。用户端保持基本响应式布局，安卓沿用“尽量正常打开、其余从简”的方向进行辅助实测，但不承诺所有安卓浏览器。

iPhone、微信内网页、原生小程序、App、切后台/锁屏后连续稳定通话，不属于本次必保能力。不可因此忽视任务回收；恢复页面时显示实际状态，不播放过期音频或假装仍连接成功。

### 17.5 上线测试前的最小运行检查

确认平台账号权限和实际接口；初始化管理员；验证数据库迁移和日志脱敏；检查照片访问权限；验证自动人物创建正常路径；验证唯一声音输出和黑屏降级；验证五秒新旧会话隔离；检查任务清理；确认测试入口可停止开放。

此检查是工程建议，不意味着已经约定源代码转让、长期备份服务、正式运维 SLA 或公开收费运营。

## 18 测试与验收用例

### 18.1 执行约定

本节是工程验收建议，围绕已定功能验证。未有客户给定的量化性能承诺不擅自补数字。每条记录版本、浏览器、供应商配置、输入素材、步骤、结果、日志 request_id 和是否通过。状态可选通过/失败/阻塞；涉及 U01、U02、U05 或未验证供应商能力的用例必须先解决条件。

倒计时边界优先使用可注入测试时钟，不必每次真实等待十五分钟；上线前再做真实时间端到端测试。并发测试使用两个独立登录环境同时发起，不能只在一个页面顺序点击。素材使用已获授权的测试人物。

### 18.2 账号与隔离

| 编号 | 步骤 | 通过标准 | 对应需求 |
|---|---|---|---|
| TC-001 | 管理员建号，用户使用发放账号登录 | 可登录；重启后账号仍存在；不出现注册/短信入口 | AUTH-01 |
| TC-002 | 尝试查询账号旧密码、查看日志和数据库内容 | 只能看到校验值或脱敏数据；密码不泄露 | AUTH-02 |
| TC-003 | 同一账号在两个浏览器登录，再同时开始通话 | 两处可登录；最多一场建立；另一处提示已有通话 | AUTH-03 |
| TC-004 | 管理员重置正在使用账号的密码 | 旧登录与依赖的实时会话失效，新密码可登录 | AUTH-03 |
| TC-005 | 用户甲直接请求用户乙的列表项、配置、会话和恢复凭证 | 全部按未授权拒绝，不返回他人数据或媒体票据 | AUTH-04 |
| TC-006 | 分享角色业务 URL 给未授权账号或未登录浏览器 | 链接本身不能赋予权限 | AUTH-04 |
| TC-007 | 两个用户依次使用同一个资源，设置不同称呼和背景 | 分别使用自己的配置；后者不知道前者聊天 | CFG-01、MEM-01 |
| TC-008 | 请求管理员接口并伪造前端角色类型 | 服务端仍按真实账号权限拒绝 | AUTH-04 |

### 18.3 人物创建与管理

| 编号 | 步骤 | 通过标准 | 对应需求 |
|---|---|---|---|
| TC-009 | 上传合格照片后关闭管理页面，等待平台处理 | 后台继续，完成后自动可用；不需人工填 ID | AV-02 |
| TC-010 | 提供不合格素材或格式不匹配文件 | 拒绝并提示原因，不自动修复/补全，不创建可通话假人物 | AV-01 |
| TC-011 | 模拟明确制作失败，点击重试 | 失败可见；重试任务关联清楚；失败计数不占 | AV-02、AV-04 |
| TC-012 | 提交超时但供应商实际已受理 | 先查询原任务，不重复制造新人物 | AV-02、AV-04 |
| TC-013 | 双击同一提交、重复发送同一幂等键 | 只创建一份本地任务及正常供应商任务 | AV-04 |
| TC-014 | 用户明确两次新建，使用同一张照片、不同提交标识 | 形成两份独立创建并按规则计数，不按 hash 强行合并 | AV-04 |
| TC-015 | 处理中和失败记录混合统计 | 处理中计一、失败计零；尚未确定额度不用于拒绝 | 04.4、U01 |
| TC-016 | 正常自动创建完成，管理员尚未测试 | 已授权且启用入口可用，不增加人工审核门槛 | AV-02、CFG-04 |
| TC-017 | 故障后采用人工兜底 | 记录异常与操作来源；不能替代自动创建验收 | AV-03 |
| TC-018 | 制作回调重复、乱序、伪造或引用其他任务 | 终态不被倒退；重复安全；非法回调拒绝 | AV-02、14.9 |

### 18.4 占用、绑定与修改

| 编号 | 步骤 | 通过标准 | 对应需求 |
|---|---|---|---|
| TC-019 | 两名不同用户同时点击同一资源 | 原子地仅一人成功；另一人收到角色被占用，不排队 | CALL-01、S3-A4 |
| TC-020 | 两个用户配置 ID 不同但指向同一个资源 | 仍按共享资源互斥，不能绕过独占 | 04.2 |
| TC-021 | 管理员在用户通话中发起测试 | 拒绝抢占；用户原通话不受影响 | CFG-04 |
| TC-022 | 资源空闲时管理员独立测试 | 使用独立上下文和任务，日志标识为测试 | CFG-04 |
| TC-023 | 用户通话中管理员修改名称、照片或任一用户口吻 | 前端提示且后端保存拒绝；旧配置不被改变 | CFG-02 |
| TC-024 | 管理员与用户同时进行保存和开始通话 | 结果串行一致，不出现已忙碌仍成功编辑的竞态 | CFG-02 |
| TC-025 | 通话中进行停用或解绑 | 必须终止受影响会话；具体先后及提示按 U02 确认后验收 | AUTH-03、U02 |
| TC-026 | 尝试为同一账号分配多个资源并计算五个名额 | 该用例先标阻塞；待 U01 确认，不自行按多对多通过 | U01 |

### 18.5 一问一答与声音

| 编号 | 步骤 | 通过标准 | 对应需求 |
|---|---|---|---|
| TC-027 | 连接后没有发声，静置1.2 秒 | 不自动提交空白发言，不无故回答 | CALL-02 |
| TC-028 | 讲话中停顿 1.8 秒后继续 | 不提前结束本轮 | CALL-02 |
| TC-029 | 有过讲话后连续静音满1.2 秒 | 仅提交一次本轮结束，模型开始回复 | CALL-02 |
| TC-030 | AI 回复期间用户插话 | 不触发新一轮、不打断回复；仍能主动挂断 | CALL-02 |
| TC-031 | 豆包输出已结束但数字人声音还在播放 | 仍是 PLAYING，不提前恢复麦克风上传 | MEDIA-01、02 |
| TC-032 | 测试不同日常问题和自然反问 | 短回答、不长篇；不因超过五十字硬切声音 | S3-A8 |
| TC-033 | 静音和取消静音，分别在监听与播放中操作 | 输入开关准确；不改变回复内容、不重复创建会话 | CALL-03 |
| TC-034 | 任何阶段点击挂断，包括连接中与倒计时中 | 及时进入结束路径，迟到回调不能重新开播 | CALL-01、05 |

### 18.6 空闲、时长和记忆

| 编号 | 步骤 | 通过标准 | 对应需求 |
|---|---|---|---|
| TC-035 | AI 播完后监听，无人声持续 119.9 秒 | 尚不出现空闲倒计时 | TIME-02 |
| TC-036 | 持续无声到 120 秒 | 显示十秒警告，而不是已经挂断 | TIME-02 |
| TC-037 | 到 130 秒仍无声且未点继续 | 结束通话，记录 IDLE_TIMEOUT 并清理 | TIME-02 |
| TC-038 | 倒计时中说话 | 立即取消倒计时，正常采集并重新计时 | TIME-02 |
| TC-039 | 倒计时中点击继续且麦克风仍静音 | 取消并重计；不自动开麦、不向模型发送空发言 | TIME-02、CALL-04 |
| TC-040 | 无声时点击空白、移动鼠标、平台眨眼或发心跳 | 不重置空闲时间 | TIME-02 |
| TC-041 | AI 长时间思考/播放且服务发生异常 | 不误当用户空闲；由单独服务超时触发处理 | ERR-02 |
| TC-042 | 本场到第 890 秒和 900 秒 | 提前十秒提示，到十五分钟结束；继续按钮无效于硬时长 | TIME-03 |
| TC-043 | 本场已记住一个测试事实，然后主动挂断并新进入 | 新场不引用旧聊天；管理员原有角色资料仍保留 | MEM-01 |
| TC-044 | 首次监听从未讲话 | 按 U05 已确认后的规则验收，暂不能擅自用1.2 秒判停回复 | U05 |

### 18.7 网络与旧场清理

| 编号 | 步骤 | 通过标准 | 对应需求 |
|---|---|---|---|
| TC-045 | 用测试时钟模拟失联 4.9 秒后恢复，原上游仍有效 | 同一通话 ID、同一上下文、原计时继续 | NET-01 |
| TC-046 | 模拟恰好 5.000 秒恢复 | 按不超过五秒处理；验证统一时间精度 | NET-01 |
| TC-047 | 模拟 5.001 秒后恢复 | 旧场终止，新通话/新上游上下文；提示不保留旧聊天 | NET-01、02 |
| TC-048 | 刷新后 1 秒内重进 | 仍是新场；不能用恢复票据续前场 | NET-01 |
| TC-049 | 网络超过五秒，旧场清理迟缓，另一用户尝试进入 | 不并行运行同一角色；显示占用/清理状态，不强行抢占 | NET-02、CLEAN-01 |
| TC-050 | 旧场已关闭，新场运行时旧媒体回调到达 | 旧声音、旧成功事件和旧释放操作被代次过滤 | NET-02 |
| TC-051 | 不到五秒但上游对话已丢失 | 明确服务异常，不能假装记忆恢复成功 | NET-01、G02 |
| TC-052 | 页面直接关闭或后端异常退出 | 有后端检测/补偿任务，不依赖正常点击挂断 | CLEAN-01 |

### 18.8 黑屏降级与资源不足

| 编号 | 步骤 | 通过标准 | 对应需求 |
|---|---|---|---|
| TC-053 | 正常视频通话中视频服务失败、豆包仍正常 | 黑屏并显示纯语音状态，后续语音继续、本场记忆和计时保留 | MEDIA-03 |
| TC-054 | AI 一句回复播到中途时触发视频故障 | 只有一份声音；无整句从头重复；衔接能力按 G03 实测 | MEDIA-04 |
| TC-055 | 数字人额度不足但人物授权和语音链路有效 | 验证可降级的启动场景；不显示角色忙碌假原因 | CAP-02、G03 |
| TC-056 | 角色被占用或未授权时故意请求纯语音 | 不能绕过独占和权限 | MEDIA-03 |
| TC-057 | 视频正常但豆包语音服务失败 | 不错误声称纯语音可继续；触发语音异常处理 | ERR-01 |
| TC-058 | 视频已降级，另一个用户尝试同一角色 | 本场语音还在使用该资源，仍禁止第二人进入 | MEDIA-03 |

### 18.9 数据、运行与界面

| 编号 | 步骤 | 通过标准 | 对应需求 |
|---|---|---|---|
| TC-059 | 连续创建超过旧版三个账号、五个系统人物 | 不被已经取消的产品上限拒绝；实际服务故障另报 | CAP-01 |
| TC-060 | 一天使用超过旧版三次 | 不出现每日次数限制；保留每场十五分钟 | CAP-01、TIME-03 |
| TC-061 | 检查三十天前后的日志并执行清理 | 过期运行明细删除，账号和角色资料仍可正常使用 | DATA-01 |
| TC-062 | 检查日志、数据库、对象存储和浏览器包 | 不出现明文密码、长期平台密钥、聊天全文或录音录像 | SEC-01、DATA-01 |
| TC-063 | 查看无报价数据的用量页面 | 时长和次数可查；成本显示未知，不显示零或已删预算告警 | COST-01 |
| TC-064 | 电脑 Chrome 实际部署地址测试权限、播放与响应式 | 主线正常，记录环境；其他设备不据未承诺项扩展验收 | 17.4 |
| TC-065 | 切后台/锁屏后返回 | 不以后台稳定为通过条件，但不误播旧媒体、不过期占用不清理 | NET-03、17.4 |
| TC-066 | 连续运行多轮并测试创建/清理失败恢复 | 无不断增长的遗留任务、锁和音频队列；具体样本量记录 | 19.3 |

共 66 条测试用例。是否所有性能和画面样本都需要客户正式签字另行确定；技术通过不能替代客户对实际人物观感的确认。

### 18.10 画面与体验测试方法

使用同一张人物照片、同一组短句/长句/停顿音频，分别记录正常视频、长时间对话和故障切换。观察嘴角、牙齿、皱纹、转头边缘是否跳变，以及开口和停止是否明显错位。素材成功率、首画面等待、每轮首声等待和同步偏差先记录基线，不捏造合格阈值。

人工观看只能确认观感；定位问题需要同步保存媒体时间信息与状态事件。录制验收样例须使用单独授权的测试流程，不因此启用正式产品的通话录音录像功能。

## 19 开发里程碑与完成定义

### 19.1 开发顺序【实现建议，不构成工期报价】

| 阶段 | 要完成的工作 | 可以判定完成的证据 |
|---|---|---|
| M0 供应商风险验证 | 照片自动创建、外部豆包音频驱动、停止任务、播放反馈与降级 | G01—G04 的真实调用记录；不是只看营销演示 |
| M1 账号与资源管理 | 管理员发账号、持久数据、自动制作状态、用户配置与权限 | 登录隔离、制作正常路径、用户列表测试通过 |
| M2 视频通话闭环 | 复用语音、接数字人、唯一音频出口、正常结束 | 真实角色多轮对话，无双声、无提前监听 |
| M3 会话规则 | 1.2 秒判停、120＋10 秒、十五分钟、五秒重连、角色独占 | 注入时钟、双用户并发和迟到事件测试通过 |
| M4 异常与运行 | 黑屏纯语音、资源回收、账号撤权、三十天日志 | 异常恢复与数据清理用例通过 |
| M5 主线体验确认 | Chrome 实机、实际人物小样、连续使用和用量核对 | 保留测试报告；客户对人物效果作明确判断 |

供应商自动创建是前置风险，应在大量界面开发前验证。M1 的账号基础可与 M0 并行，但不能在 G01 失败时把人物日常人工制作冒充最终自动流程。

### 19.2 主线完成条件

一个合格角色从管理员上传开始自动走到可用，授权用户正确看到入口；语音与数字人真实接通；按用户配置回复；无跨用户聊天串用；挂断、超时、重置和异常后能结束与回收；视频故障可进入已确认的黑屏纯语音路径；取消的旧人数/费用/短信规则没有残留。

单场可演示不等于稳定完成；公共平台示例角色可说话不等于客户人物自动创建完成；纯语音一直正常不等于数字人已完成；DB 有状态不等于平台任务已释放。

### 19.3 验证规模建议

建议先用少量获授权角色、两个独立用户环境验证隔离和竞态，再做连续多轮与持续运行测试。测试记录实际轮数、时长与峰值，不把建议使用的少量测试账号重新做成产品总量限制。

尚未约定正式上线并发、服务可用性、网络条件和最终延迟阈值，故本版不出具“可承载多少正式用户”的结论。费用和部署采购在主线测试通过后用实际数据评估。

### 19.4 完成状态的记录方式

每个模块分为未开始、开发中、可内部测试、已通过、待确认/外部阻塞。外部阻塞需写具体缺失资料，不以“平台问题”笼统结束。内部模拟可用于页面开发，但必须显式标识，不能计入真实接入验收。

## 20 未决事项与技术验证清单

### 20.1 需要最终确认的产品边界

| 编号 | 未决内容 | 本版处理与建议 |
|---|---|---|
| U01 | B2 改多对一，B5 又保留额度语义；此前每账号五个入口是否还适用？ | 已确认多个用户可共享一资源。先按 B2 描述，不启用任意多对多；五个入口不作为拒绝条件。需明确一账号是否仅一资源，以及制作计数归属。影响绑定界面、唯一约束和 TC-026。 |
| U02 | 通话中禁改所有相关信息，与停用/解绑终止会话的操作先后 | 内容编辑明确拦截。建议安全撤权单独按“撤销继续访问→结束受影响通话→完成绑定/状态变更”；不借普通改口吻强制踢人。需确认提示和原子步骤。 |
| U03 | 系统资源不足时是否统一不排队；首次数字人失败是否自动纯语音 | 角色占用不排队已定，视频运行中黑屏纯语音已定。其他容量和首次启动失败建议不排队、满足语音与授权条件则降级，否则明确失败；属于尚未单独确认的边缘处理。 |
| U04 | 本轮回复已播放一半、视频故障且无法确定已播放边界 | 黑屏继续语音目标已定；无损切换未承诺。建议无法对齐时不重播整段，提示中止当前残余播放并保持下一轮可用；需结合 G03 决定。 |
| U05 | 首次连接后从未讲话、还没有 AI 回复时的空闲起点 | 建议从首次 LISTENING 也执行 120＋10 秒；后续轮次从 AI 实际播放结束计时已定。 |

旧的 15 分钟并未被撤销，故本版沿用，不将它再次列作已经冲突的事项。若要取消，应单独改 D060/D061，而不是从“取消人数限制”推导。

### 20.2 需要真实服务验证的关口

| 关口 | 要得到的证据 | 不通过时的处理 |
|---|---|---|
| G01 自动人物创建 | 上传素材、取得创建任务、跟踪状态、获得人物编号全过程可由程序完成；有失败查询能力 | 标记正常人物创建阻塞；人工只为异常兜底，不能作为日常替代；换供应商需重新确认 |
| G02 现有语音与短断恢复 | 现有豆包会话在五秒短断内可保留；实际声音格式可用于驱动画面 | 不承诺记忆恢复/实时视频已完成，定位适配能力或说明平台约束 |
| G03 播放完成与语音降级 | 能识别最终输出结束并切换唯一音频出口；中途故障策略有真实测试 | 先解决播放与回退策略，不能用“上传完成”冒充播放完成 |
| G04 停止、错误及额度 | 实际错误分类、任务停止确认、遗留任务查询、可用配额 | 使用已查证结果配置保护；无法确定释放时保留待清理状态 |
| G05 目标人物质量 | 客户素材的嘴型、面部微动和身份稳定性被认可 | 返工或调整方案；不通过增加新动作/情绪范围掩盖原问题 |

当前对话没有提供已核实的即构自动创建接口、测试权限或实际完整报价。本文既不宣布即构一定不支持，也不宣布已经支持。以上关口必须在真实账号下验证。技术限制不能通过改需求文字消失。

### 20.3 待配置而非待新增功能

照片准入参数、各服务异常超时、合法音频格式、播放缓存与恢复策略、接口错误映射、登录有效期、声音阈值、实际 Chrome 版本、有限清理重试策略，需要开发联调确定。全部记录配置版本，避免不同环境各用不同默认值而难以复现。

## 21 新旧决策追踪与变更记录

### 21.1 覆盖关系

| 主题 | 旧清单/旧稿 | 最新依据与本版处理 |
|---|---|---|
| Demo 定位 | S1-D001/D002：内部验证、不付费运营 | 继续保留；不自动升级正式产品 |
| 系统人物/账号数量 | S1-D003/D004：五个人物、三个账号 | S3-A2 全部取消；不得残留验证拦截 |
| 固定并发 | S1-D005：两路 | S3-A2 取消固定两路；实际资源仍有技术边界 |
| 每日人数估计 | S1-D008：十人/日 | 不作为容量承诺或访问限制 |
| 判停 | 原 S1-D011：两秒 | 2026-09-09 当前阶段改为1.2秒 |
| 插话 | S1-D012：不支持 | 保留，挂断始终可用 |
| 音色 | S1-D017：选择存在歧义 | S2-4 明确默认音色、不克隆 |
| 回复长度 | S1-D018：三句、每句五十字 | S3-A8 改为短回答短反问风格，不是硬字数 |
| 长期配置/记忆 | S1-D019—D026 | 配置持久、单场上下文、新场不恢复、无知识库 |
| 自然画面 | S1-D027—D030 | 保留第三方驱动、自然微动，不做指定动作 |
| 情绪表情 | S1-D031/D032 必须内容对应 | S2-8 明确舍弃，不作为 P0 或平台阻塞项 |
| 人物取景 | S1-D033 要看到手臂 | S2-14 正脸、肩部以上即可 |
| 用户摄像头 | S1-D034 不做 | 继续不做 |
| 照片处理 | S1-D035—D038 | 保留上传/准入；不合格退回，不自动修复 |
| 人工与自动创建 | S1-D039/D040 同时选是 | S2-1/11：自动为正常流程，人工仅异常兜底 |
| 发布/测试 | S1-D041；S2-11 | 完成自动可用；S3-B4 管理员测试不是发布必经审核 |
| 创建时限与调整次数 | S1-D042/D043 未设置固定限制 | 不擅自新增完成时间或无限免费返工承诺 |
| 账号来源 | S1-D046—D048 含自助注册、短信 | S2-2/13、S3-A6：全部管理员分发和重置 |
| 多设备 | S1-D058 不许多设备通话 | S3-A6：可多登录但一账号一场，旧登录可撤销 |
| 关系与五个入口 | S1-D053、S2-13 为多对多/最多五个 | S3-B2 改多对一；S3-B5 仍有计数，U01 待收口 |
| 角色独占 | S1-D054/D055 | S3-A4、B3 强化：同资源不并用，管理员不抢占 |
| 角色修改 | 旧建议可能允许修改下一场生效 | S3-B9：通话中禁止改；与安全撤权顺序见 U02 |
| 单次时长 | S1-D060/D061 十五分钟、提前十秒 | 未被 A2/B7 撤销，继续沿用 |
| 无人互动 | S1-D062 有歧义，旧稿曾按110/120 | S3-A5：120 秒开始提醒，追加10秒，130秒结束 |
| 每日限制 | S1-D063 三次/日 | S3-A2/B7 明确取消；不做日累计限时 |
| 整体累计时长 | S1-D065 未决定 | 本版不启用，也不虚构新的总分钟上限 |
| 排队 | S1-D066 提供排队 | S3-A4 角色忙碌不排队；其余容量情况建议同样简化，见 U03 |
| 设备 | S1-D068—D071 均非必须 | S2-9＋S3-A7：Chrome 主线，其他从简；无全设备承诺 |
| 后台 | S1-D072 不保证 | S3-B11 不保证后台持续或返回后稳定通话，不开发专项恢复 |
| 重连 | S1-D073 十秒 | S3-A3 改五秒；超过五秒新场，不恢复旧聊天 |
| 视频故障 | S1-D074 不纯语音降级 | S3-B12 明确黑屏继续纯语音 |
| 日志 | S1-D075 无期限 | S3-B10 三十天；不保存完整聊天/录音录像 |
| 费用 | S1-D084/D085 40元/人、10元/十分钟，D091 预警 | S2-5 取消测试期费用上限与预算功能，主线通过后评估 |
| 关闭测试入口 | S1-D006 不设期限 | S2-12 保留开发方关闭权；正式长期使用另议 |
| 基础保障 | S1-D113—D120 | 继续实施，不因缩小 Demo 而撤销 |

### 21.2 给开发者的最终核对

不得重新加入：短信、自助注册、声音克隆、指定动作、内容情绪控制、任意多对多、固定两路、每日三次、十秒重连、110秒空闲提醒、费用预算拦截、不降级纯语音等被撤销规则。

不得误删：角色和账号持久数据、用户隔离、独占、管理员重置、单次十五分钟、1.2 秒判停、必要用量、异常任务回收、AI 身份说明。

不得假装已确认：每账号五个入口最终基数、额度归属、即构自动创建可用性、无损半句音频接力、供应商实际配额、具体性能 SLA 和部署报价。

### 21.3 来源索引

S1：当前上传文件 `ai_avatar_demo_decision_checklist(1).md`，保留原 D 编号用于定位；原文件已删除的 12/13 类章节不恢复为客户承诺。

S2：本轮对话中用户以“现在我进行补充”开头的 1—14 项，以及“删除短信验证码登录和找回密码，一律由管理员在后台分配和修改”。

S3：本轮对话中用户以“A2，这里的全部取消，先不考虑。先跑通”开头的 A/B 补充，至“B12：数字人出错则回退为黑屏无画面。继续纯语音”。

本文中的技术实现和验收设计依据这些已定行为推导，并已标识为实现建议；不将助手历史平台推荐、估算或未被用户接受的默认值替代用户决定。

### 21.4 后续版本更新规则

确认 U01/U02 等边界后更新为 V0.3 或相应冻结版；实际供应商接入报告作为附录保存。任何改变单次时长、用户关系、音色、记忆、动作、并发产品规则或付费运营定位的修改，需记录来源和受影响测试用例。

本文件同时提供 Markdown 与 Word 版本，内容保持一致。界面视觉稿、供应商正式接入文档、部署密钥、实际测试报告不内嵌到 PRD 正文，避免把临时配置或秘密信息作为公开交付内容。
