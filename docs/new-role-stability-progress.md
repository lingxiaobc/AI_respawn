# PT-new-role-stability v1 执行记录

> 2026-09-06修正：下文“90多岁男性预检拒绝”是旧流程的历史结果，不应解释为该角色无法规范化。现已移除原图精确定位门槛，该原图基础检查通过；尚未生成其标准图或验证动画。严格定位改为针对标准图，见[修正记录](canonical-gate-fix-2026-09-06.md)。历史报告保留。

## 最新更新：Gemini三位新角色已完成技术验收

用户改为Gemini并追加累计40次额度后，已完成全部5图验证：3个新角色同版12阶段通过、1个Gemini连接中断暂停、1个输入预检拒绝。三位成功角色Ubuntu CPU复验及网页上传验证通过，身份/自然度仍待用户确认。累计37/40，本轮17次请求16次图像成功，未重发未知失败。完整证据和通用修复见[Gemini稳定性验证](gemini-stability-validation-2026-09-05.md)，图表汇总为`AI_output/gemini-stability-review/evidence.json`。以下保留先前GPT阶段的历史记录，不代表当前整体状态。

## 历史记录：已授权的一次GPT重试仍未收到响应

用户随后明确授权重试并继续，已记录为执行版本2：只增加首图normalize一次手动替代尝试，总上限仍39。原失败账本保留，新记录为`AI_output/motion/.calls/d3182972ea563c4a8cf8d1ab2e96789a5d6545b2c77bd9bd3a1e4fa5739d04b5-approved-retry-1.json`。

- 当前普通沙箱内无密钥GET仍EACCES，因此没有仅凭用户“应该关闭了沙箱”就假定实际权限已改变。
- 已通过执行权限工具在沙箱外运行完整首图任务，创建当前代码版本的新角色`aca14f24-e83f-4a90-a476-3fbdb30c9f80`；原角色与原失败没有删除。
- 请求UTC开始2026-09-05T14:50:50.140Z，结束14:51:50.243Z；北京时间22:50:50.140至22:51:50.243。
- 实际错误：`[stage=request-awaiting-headers http=none elapsedMs=60097] (UND_ERR_SOCKET) fetch failed`，phase=request，没有requestId、响应头或图像。
- 这是与之前29ms EACCES不同的连接中断；权限放行不等于平台生成成功。无法据此判断断开的责任方或是否计费，需核对该时段平台记录。没有发起第二次重试，也没有因GET成功认定生图可用。
- 累计尝试账本16/39。根.env付费开关恢复0，密钥未修改/输出。显式replacement若失败，普通重入仍被账本拒绝；若成功则只复用缓存，不消耗第二次授权。
- 新增通用无付费缓存回归入口`scripts/role-replay.ts`，强制fixture和禁止provider；可从原始输入重新执行完整12阶段而不覆盖源角色。
- 当前88项Node测试及类型检查通过；新替代记录的原账本保留、禁付费与一次调用后缓存复用测试通过。青年当前版完整回归12阶段，63帧遮罩外像素0。女性结果另记任务追踪。

**仍未达到本轮至少3张全新角色通过的目标。A-03被首图规范化中断阻塞，不越过计划依赖直接用其他图片替代成功率；A-04/A-05未完成。** 若平台已生成成功，应先核对并恢复该次原始结果；若没有可恢复结果，再明确下一次尝试边界，不自动重发。

## 当前结果（2026-09-05）

已获用户批准：5张新增图最多25次首次生图，共享累计上限39次，不自动重试。根.env为唯一项目配置源。任务包见TASK/in-progress或TASK/stopped/new-role-stability。

免费预检脚本：`scripts/portrait-motion/stability_preflight.py`；完整结果、源哈希、代码基线见`AI_output/stability-v1/preflight-matrix.json`。

| 输入 | 预先分类 | 当前结果 |
| --- | --- | --- |
| 40多岁男性.png | 首个正向开发样本 | 原图通过；首次规范化请求EACCES暂停 |
| 50多岁女性.png | 正向保留样本 | 原图通过，尚未生图 |
| 50多岁男性侧身.png | 边界样本 | 原图通过，尚未生图 |
| 50多岁，女性侧身.png | 边界样本 | 原图通过，尚未生图 |
| 90多岁男性.png | 远景小脸边界样本 | 原图被拒绝：Exactly one detectable face is required；不声称人脸实际不存在，只说明当前检测器不能可靠定位 |

6张旧图预检均通过；隔离生成的模糊、多人和遮挡负例均拒绝。未改原图；拒绝项不计为动画生成成功。新图尚无完整动画通过，不满足至少3张全链路验收目标。

## A-02加固及验证

- 付费账本区分request、response-headers、response-received、save-image、validate-image和commit-success；保存requestId（若有）、接收哈希/字节数、失败阶段；完整返回后落盘失败不会冒充平台未生成。
- 同ID失败/未知状态仍在调用provider前拒绝，进程重启不会自动重发。
- 新任务记录代码/模型指纹，已有阶段与当前指纹不符时暂停。依赖包含提示词、生成合同、缓存、校验、接收、Python脚本和模型文件；不改写旧资源或旧账本。需要重建时创建新任务复用有效图片缓存。
- 87项Node测试、12项Python测试、TypeScript检查通过。故障注入覆盖头前断开、实际流读取断开、落盘失败、坏图片、重复提交、重启和旧版本恢复。
- 独立复核提出指纹缺少提示词等依赖，已补齐并加入逐项依赖变化测试。新旧真实资源全量回放与Ubuntu验证尚未执行，不冒报完成。

## 首次新规范化请求及权限证据

角色：`a3b2051a-e812-4f46-a4d9-dffe1f8fcd97`。首次normalize耗时29ms，日志：`[stage=request-awaiting-headers http=none elapsedMs=29] (EACCES) fetch failed`。

随后的无密钥、不生图GET对照采用同一个URL `https://zenmux.ai/api/v1/models`：

- 沙箱内：53ms，`connect EACCES 198.18.0.38:443`，未取得HTTP头。
- 经执行权限批准的沙箱外：432ms，HTTP200。

这是当前沙箱内连接访问被拒绝的直接证据，不是平台业务报错；该对照不能证明生图POST已经成功，更不能反推历史ECONNRESET的根因。不能从GET成功推断模型权限或账户额度。

账本累计15次（新增1次失败/未知预留），保留原记录；未绕过账本再请求。按批准计划，首图A-03通过前不进入A-04，不偷偷改换首图。请求重试不在本轮首次调用授权内，需明确一次性授权；不需要提高累计39次上限，但会修订首图最多5次首次尝试的执行边界。

暂停前将根.env的ROLE_ALLOW_PAID恢复0；测试专用输出目录及浏览器配置保留。没有修改/输出密钥，没有提交、推送、部署或改全局网络规则。

## 继续条件

用户明确授权在沙箱外对首图normalize手动重试一次。恢复时先固定新代码指纹、验证已有源图和预检证据；保留失败账本，通过显式替代尝试记录关联原请求，不删除/改写历史失败来绕过重复调用保护。成功后继续同一角色剩余四态和后续样本；仍不自动重试任何其他失败请求。
