# 验收报告（v2 修复后，连续性验收中）

日期：2026-08-30
基准：Windows Chrome；10 轮至少 9 轮成功；松开到首声 P95 不高于 3 秒。

## 已通过

| Gate | 证据 | 结论 |
| --- | --- | --- |
| Gate 0 | 官方协议、音频契约、状态机、密钥边界与阈值已固化 | 通过 |
| Gate 1 | 固定公开样本直连完整会话；修复后真实探针首包约 937 ms；输出经 Float32LE→PCM16LE 规范化后结构和波形合法 | 通过 |
| Gate 2 / 输入 | 默认 Intel 麦克风本地 4.02 秒检查非静音；CLI PTT 5/5 轮采集有效，五轮 ASR 均完成 | 通过 |
| Gate 2 / 输出 | 修复后用户确认浏览器可正常听到人声；固定输出 WAV 为 24 kHz/mono/16-bit，peak 9979、RMS 2246 | 单轮通过，5 轮统计待完成 |
| Gate 3 / 网关协议 | 网关已在可访问上游的进程中重启，用户确认页面打开并正常播放语音 | 单轮通过，5 轮统计待完成 |
| 离线质量 | TypeScript 检查、17 项协议/音频/状态/脱敏测试、Vite 生产构建 | 通过（未覆盖真实位深语义） |

## 历史失败与当前待完成项

- v1 历史失败：原始 payload 按 Int16 读取呈现交替零值和尖峰；按 Float32LE 读取为有限、连续值，导致此前 WAV/队列验收被撤销。
- v2 当前结论：provider 规范化和真实固定样本已通过，用户已确认浏览器能正常输出人声；剩余工作是按原基准完成 5 轮连续性记录。

## 待完成

- 修复后在系统 Chrome 完成剩余浏览器连续性轮次，累计达到 5 轮真实按下/松开、AudioWorklet 输入与 Web Audio 输出听感验证。
- 按原基准补齐 10 轮成功率与 P95 首声统计；在此之前不宣称完整商业验收完成。

在这些项目完成前，不把 Gate 4 标为通过，也不进入声音复刻、Avatar 或 RAG。

## 安全检查

- `.env` 被 `.gitignore` 排除；只检查过密钥变量非空，未打印其值。
- 浏览器生产 bundle 扫描未发现 `DOUBAO_API_KEY` 或 `X-Api-Key`。
- 日志不记录转写正文、Base64 音频或麦克风原始数据。
- 真实麦克风输入未落盘；探针输出只对应公开固定样本，位于被忽略的 `artifacts/`。
