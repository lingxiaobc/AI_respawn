# 验收报告（v2 修复后，已完成）

日期：2026-09-01
基准：Windows Chrome；v2 要求完成 5 轮连续性验收，确认无尖刺、变速、重叠、长断裂或明显截断。

## 已通过

| Gate | 证据 | 结论 |
| --- | --- | --- |
| Gate 0 | 官方协议、音频契约、状态机、密钥边界与阈值已固化 | 通过 |
| Gate 1 | 固定公开样本直连完整会话；修复后真实探针首包约 937 ms；输出经 Float32LE→PCM16LE 规范化后结构和波形合法 | 通过 |
| Gate 2 / 输入 | 默认 Intel 麦克风本地 4.02 秒检查非静音；CLI PTT 5/5 轮采集有效，五轮 ASR 均完成 | 通过 |
| Gate 2 / 输出 | 修复后用户确认浏览器可正常听到人声；固定输出 WAV 为 24 kHz/mono/16-bit，peak 9979、RMS 2246 | 通过（用户确认 5 轮连续性验收已完成） |
| Gate 3 / 网关协议 | 网关已在可访问上游的进程中重启，用户确认页面打开并正常播放语音 | 通过（用户确认 5 轮页面往返验收已完成） |
| 离线质量 | TypeScript 检查、17 项协议/音频/状态/脱敏测试、Vite 生产构建 | 通过（未覆盖真实位深语义） |

## 历史失败与当前结论

- v1 历史失败：原始 payload 按 Int16 读取呈现交替零值和尖峰；按 Float32LE 读取为有限、连续值，导致此前 WAV/队列验收被撤销。
- v2 当前结论：provider 规范化、真实固定样本和浏览器连续性验收均已通过；用户确认此前已完成要求的 5 轮测试，未发现尖刺、变速、重叠、长断裂或明显截断。

## 用户确认

- 2026-09-01：用户确认修复及正式浏览器验收早已完成，之前的 T-09/T-10 只是任务跟踪器遗漏勾选。
- 据此关闭 v2 的 Gate 2 输出和 Gate 3 连续性验收；本报告不追加未经记录的轮次或性能数字。

更长周期的 10 轮成功率/P95 统计（如未来仍需要）属于单独的性能观察项，不阻塞本 v2 修复任务关闭。项目下一条主线是 Live2D 资源与前端结合。

## 安全检查

- `.env` 被 `.gitignore` 排除；只检查过密钥变量非空，未打印其值。
- 浏览器生产 bundle 扫描未发现 `DOUBAO_API_KEY` 或 `X-Api-Key`。
- 日志不记录转写正文、Base64 音频或麦克风原始数据。
- 真实麦克风输入未落盘；探针输出只对应公开固定样本，位于被忽略的 `artifacts/`。
