# 验收报告（进行中）

日期：2026-08-30
基准：Windows Chrome；10 轮至少 9 轮成功；松开到首声 P95 不高于 3 秒。

## 已通过

| Gate | 证据 | 结论 |
| --- | --- | --- |
| Gate 0 | 官方协议、音频契约、状态机、密钥边界与阈值已固化 | 通过 |
| Gate 1 | 固定公开样本连续 3 次直连完整会话；首包 908/984/887 ms；三份 24 kHz PCM16 WAV 均非静音且结构合法 | 通过 |
| Gate 2 / 输入 | 默认 Intel 麦克风本地 4.02 秒检查非静音；CLI PTT 5/5 轮采集有效，五轮 ASR 均完成 | 通过 |
| Gate 2 / 输出 | 默认 Realtek 扬声器 5/5 轮完成队列播放；首写 1002/947/935/757/926 ms | 通过 |
| Gate 3 / 网关 | 固定浏览器协议流完成 121 帧输入、365104 bytes 输出、playback done、返回 ready 与关闭；首包 920 ms | 通过 |
| 离线质量 | TypeScript 检查、17 项协议/音频/状态/脱敏测试、Vite 生产构建 | 通过 |

## 待完成

- 在系统 Chrome 中允许麦克风并完成真实页面按下/松开、AudioWorklet 输入与 Web Audio 输出验证。
- 浏览器端连续 10 轮统计成功率、P95 首声延迟与主观听感。
- 用户确认鼠标/空格输入、权限拒绝提示、播放锁定与可重试错误符合预期。

在这些项目完成前，不把 Gate 3 浏览器音频与 Gate 4 标为通过，也不进入声音复刻、Avatar 或 RAG。

## 安全检查

- `.env` 被 `.gitignore` 排除；只检查过密钥变量非空，未打印其值。
- 浏览器生产 bundle 扫描未发现 `DOUBAO_API_KEY` 或 `X-Api-Key`。
- 日志不记录转写正文、Base64 音频或麦克风原始数据。
- 真实麦克风输入未落盘；探针输出只对应公开固定样本，位于被忽略的 `artifacts/`。
